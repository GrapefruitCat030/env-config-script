" Modified at 9/14/2023.
" Generated by GPT-3.5 from my vscode's setting json, in which a vim setting valids.

set tabstop=4        " 设置制表符宽度为4个空格
set shiftwidth=4     " 设置自动缩进宽度为4个空格
set expandtab        " 将制表符自动扩展为空格


" 启用 EasyMotion 插件
let g:vim_easymotion = 1

" 启用增量搜索
set incsearch

" 使用系统剪贴板
" set clipboard=unnamedplus

" 启用使用 Ctrl 键的快捷键
" set mouse=a

" 启用高亮搜索结果
set hlsearch

" 定义 leader 键为 <Space>
let mapleader = "\<Space>"

" 设置相对行号
set relativenumber

" 在插入模式下，使用 jj 映射为 <Esc>
inoremap jj <Esc>
" 在插入模式下，使用 JJ 映射为 <Esc>
inoremap JJ <Esc>

" 在普通模式下，将 H 映射为 ^
nnoremap H ^
" 在普通模式下，将 L 映射为 $
nnoremap L $
" 在普通模式下，使用 <C-n> 来取消搜索高亮
nnoremap <C-n> :nohl<CR>
" 在普通模式下，将 J 映射为 j
nnoremap J j
" 在普通模式下，使用 K 来插入新行
nnoremap K o
" 在普通模式下，使用 zz 来切换折叠
nnoremap zz :editor.toggleFold<CR>

" 在可视模式下，将 H 映射为 ^
vnoremap H ^
" 在可视模式下，将 L 映射为 $
vnoremap L $
" 在可视模式下，使用 <C-n> 来退出可视模式
vnoremap <C-n> <Esc>
" 在可视模式下，使用 <Tab> 来增加缩进
xnoremap <Tab> >gv
" 在可视模式下，使用 Shift-Tab 来减少缩进
xnoremap <S-Tab> <gv
